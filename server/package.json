{
  "name": "danubria",
  "version": "1.0.0",
  "description": "Fullstack Danubria app (client + server)",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^6.9.13",
    "path": "^0.12.7"
  }
}
