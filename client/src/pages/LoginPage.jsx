import React, {useState} from 'react'
import axios from 'axios'
export default function Login(){ const [u,setU]=useState('traffic.acbutescu@gmail.com'), [p,setP]=useState('Porumbeiimei112'); async function submit(e){ e.preventDefault(); try{ const r=await axios.post('/api/auth/login',{ username:u, password:p }); localStorage.setItem('accessToken', r.data.accessToken); alert('Logged in'); window.location.href='/dashboard' }catch(e){ alert('Login failed') } } return (<div style={{maxWidth:480, margin:'40px auto'}}><h1>Login</h1><form onSubmit={submit}><div><label>Email</label><input style={{width:'100%', padding:8}} value={u} onChange={e=>setU(e.target.value)}/></div><div style={{marginTop:8}}><label>Password</label><input type='password' style={{width:'100%', padding:8}} value={p} onChange={e=>setP(e.target.value)}/></div><div style={{marginTop:12}}><button style={{background:'#0b74d1', color:'#fff', padding:'10px 14px', borderRadius:6}}>Login</button></div></form></div>) }
